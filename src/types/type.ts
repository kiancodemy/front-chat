export type toastType = {
  status: string;
  message: string;
};

export type singupType = {
  username?: string;
  email?: string;
  password?: string;
};
export type loginType = {
  email?: string;
  password?: string;
};
